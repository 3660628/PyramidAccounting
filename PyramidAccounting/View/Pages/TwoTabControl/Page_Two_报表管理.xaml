<Page x:Class="PA.View.Pages.TwoTabControl.Page_Two_报表管理"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="570" d:DesignWidth="800"
	Title="Page_Two_报表管理">

    <Grid>
        <StatusBar Height="25" Margin="0" VerticalAlignment="Bottom" Background="DarkGray"/>
        <TabControl Margin="0,0,0,25" BorderBrush="White">
            <TabItem Header="资产负债表" Margin="20,-2,-20,-1" Template="{DynamicResource TabItemControlTemplate_TwoTabHeader}">
                <Grid Background="White">
                    <ComboBox x:Name="ComboBox_期数" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="120"/>
                    <Button Content="打印" HorizontalAlignment="Right" Margin="0,10,100,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
                    <Button Content="导出" HorizontalAlignment="Right" Margin="0,10,20,0" VerticalAlignment="Top" Width="75"/>
                    <DataGrid x:Name="DataGrid_资产负债表" ItemsSource="{Binding}" AutoGenerateColumns="False" HorizontalAlignment="Stretch" Margin="10,60,0,0" VerticalAlignment="Top" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Col_01}" Header="科目编号" />
                            <DataGridTextColumn Binding="{Binding Col_02}" Header="资产部类"/>
                            <DataGridTextColumn Binding="{Binding Col_03}" Header="年初数" />
                            <DataGridTextColumn Binding="{Binding Col_04}" Header="期末数" />
                            <DataGridTextColumn Binding="{Binding Col_05}" Header="科目编号" />
                            <DataGridTextColumn Binding="{Binding Col_06}" Header="负债部类"/>
                            <DataGridTextColumn Binding="{Binding Col_07}" Header="年初数" />
                            <DataGridTextColumn Binding="{Binding Col_08}" Header="期末数" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <Label Content="制表单位：" HorizontalAlignment="Left" Margin="10,36,0,0" VerticalAlignment="Top"/>
                    <TextBlock x:Name="TextBlock_制表单位" HorizontalAlignment="Left" Margin="80,40,0,0" TextWrapping="Wrap" Text="值1" VerticalAlignment="Top"/>
                    <Label Content="单位：元（列至角分）" HorizontalAlignment="Right" Margin="0,38,10,0" VerticalAlignment="Top"/>
                </Grid>
            </TabItem>
            <TabItem Header="收入支出总表" Margin="30,-2,-30,-1" Template="{DynamicResource TabItemControlTemplate_TwoTabHeader}">
                <Grid Background="White"/>
            </TabItem>
            <TabItem Header="行政费用支出明细表" Margin="40,-2,-40,-1" Template="{DynamicResource TabItemControlTemplate_TwoTabHeader}">
                <Grid Background="White"/>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
